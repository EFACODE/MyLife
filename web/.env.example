# Base URL for the My Life API. Leave empty to use the same origin — in `npm run
# dev` the Vite proxy (vite.config.ts) forwards API paths to the backend
# (default http://127.0.0.1:8000; edit the target in vite.config.ts if needed).
VITE_API_BASE_URL=
